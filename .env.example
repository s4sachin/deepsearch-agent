# Google Gemini AI
GOOGLE_GENERATIVE_AI_API_KEY=your-gemini-api-key

# Database - Supabase PostgreSQL
# Transaction pooler (recommended for serverless/Next.js)
# DATABASE_URL=postgresql://postgres.[PASSWORD]@[PROJECT-REF].pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct connection (use for migrations)
DATABASE_URL=postgresql://postgres.[PASSWORD]@[PROJECT-REF].supabase.com:5432/postgres

# Redis
REDIS_URL=redis://localhost:6379

# Serper API (for web search)
SERPER_API_KEY=your-serper-api-key

# Search Configuration
# Number of search results to return from web search (defaults to 10 if not set)
SEARCH_RESULTS_COUNT=10

# Auth Secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your-secret-here

# Discord OAuth (get from: https://discord.com/developers/applications)
AUTH_DISCORD_ID=your-discord-client-id
AUTH_DISCORD_SECRET=your-discord-client-secret

# Langfuse Observability (optional)
LANGFUSE_SECRET_KEY=your-langfuse-secret-key
LANGFUSE_PUBLIC_KEY=your-langfuse-public-key
LANGFUSE_BASEURL=https://cloud.langfuse.com

# Environment
NODE_ENV=development

# Legacy Azure OpenAI (kept for backwards compatibility in env validation)
# Not actively used - the system now uses Google Gemini
AZURE_RESOURCE_NAME=placeholder
AZURE_API_KEY=placeholder
AZURE_DEPLOYMENT_NAME=placeholder
